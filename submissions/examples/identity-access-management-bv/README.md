# Identity & Access Management Center

A premium enterprise IAM dashboard with access token logs, multi-factor verification checks, and active security policy audits using pure CSS.

## Usage
Open `demo.html` directly in a browser — no server needed.

## Why EaseMotion CSS?
Demonstrates premium CSS layout, pulse animations, hover transitions, and fade-up effects with zero JS dependencies.