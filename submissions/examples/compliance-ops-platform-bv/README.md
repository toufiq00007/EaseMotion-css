# Compliance Operations Platform

A premium enterprise compliance dashboard with regulatory compliance checklists, risk evaluations, and security logs using pure CSS.

## Usage
Open `demo.html` directly in a browser — no server needed.

## Why EaseMotion CSS?
Demonstrates premium CSS layout, pulse animations, hover transitions, and fade-up effects with zero JS dependencies.